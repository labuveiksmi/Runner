using UnityEngine;

[System.Obsolete]
public class Board : MonoBehaviour
{
    //[SerializeField] private float forceBounce = 2.5f;
    //[SerializeField] private bool isLeftBoard = true;

    //private void OnCollisionEnter(Collision collision)
    //{
    //    if (collision.transform.tag.Equals(ConstantsStrings.PlayerTag))
    //    {
    //        collision.transform.GetComponent<Rigidbody>().AddForce((isLeftBoard ? Vector3.right : Vector3.left) * forceBounce, ForceMode.Impulse);
    //    }
    //}
}